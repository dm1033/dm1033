# Server-side only — NEVER commit a real key, never prefix with NEXT_PUBLIC_.
# Copy to .env.local for local dev; set as a project env var on Vercel/Netlify.
# Get a key at https://platform.claude.com — powers the AI CV review at
# /services/ats-cv-review (the feature degrades gracefully if unset).
ANTHROPIC_API_KEY=
